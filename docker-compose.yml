services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - APP_BACKEND_URL=${APP_BACKEND_URL}
    ports:
      - "8080:8080"
    depends_on:
      - backend
    env_file:
      - .env
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8005:8005"
    env_file:
      - .env
    restart: unless-stopped
