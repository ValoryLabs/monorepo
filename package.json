{
  "name": "valory",
  "version": "0.0.0",
  "private": true,
  "overrides": {
    "rollup": "4.52.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "^2.6.0"
  },
  "packageManager": "bun@1.2.21"
}
