{
  "name": "valory-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "postinstall": "uv pip install -r pyproject.toml",
    "dev": "bash -c 'cd ../backend && source .venv/bin/activate && uvicorn main:app --reload'"
  }
}
